<!DOCTYPE html>

<html>
    {{>head}}

    <body>
        <div class="main-content">
            {{>header}}
            <p>{{aboutText}}</p>
            <img src="/img/rider.jpg" alt="bitcorn chart" class="chart" />
        </div>

        {{>footer}}
    </body>
</html>